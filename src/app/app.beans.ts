export { ComponentProps, ComponentRef } from '@ionic/core/dist/types/interface';

export interface FirestoreId {
  id?: string;
}

export interface ComponentInfo {
  icon: string;
  path: string;
}

export interface ModalDismissData {
  data: any;
  role: any;
}
